import { SlidableDirective } from './slidable.directive';

describe('SlidableDirective', () => {
  it('should create an instance', () => {
    // const directive = new SlidableDirective();
    // expect(directive).toBeTruthy();
  });
});
